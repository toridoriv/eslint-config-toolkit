import { createRequire } from "node:module";

const require = createRequire(import.meta.url);

export async function importOrRequire<T>(path: string): Promise<T> {
  let mod: any = {};

  try {
    mod = await import(path);
  } catch {
    mod = require(path);
  }

  let hasDefault = "default" in mod;

  return hasDefault ? mod.default : mod;
}

// git cherry-pick 20f5331fe18a8bbf33fcd4112b2ac4d2bbb40153..9ea07db552348796a4f570f68d139fab09f95c69
